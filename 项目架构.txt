CyberMatchmaker/

|——main.go
├── config/                   # 配置管理
│   ├── config.yaml           # 配置文件 (DB, Redis, MQ, LLM Keys 都在这里)
    |——prompt.yaml  # 存放大模型的prompt
│   └── config.go             # 使用 Viper 等库解析 YAML 的结构体
├── controller/              # [MVC - C] 控制器层：处理 HTTP 请求，参数校验，调用 Service
│   ├── user.go    # 登录、注册接口
│   ├── fortune.go # 提交算命参数、轮询查询算命状态
│   └── match.go   # 发起匹配、退出匹配
├── service/                 # 业务逻辑层：承上启下，处理核心计算
│   ├── user.go       # 密码比对、Token 生成
│   ├── fortune.go    # 算命业务主干（发消息到 MQ）
│   ├── match.go      # 匹配业务主干（前置条件校验、组装数据）
│   └── llm.go        # 封装与 OpenAI 等大模型/Embedding API 的交互逻辑
├── mapper/               # 数据访问层：隔离 GORM 和 pgvector 的复杂 SQL 操作
│   ├── user.go
│   ├── fortune.go
│   └── match.go         # 封装余弦相似度检索 (vector_cosine_ops) 的查询方法
├── model/                   # [MVC - M] 模型层：数据库表结构定义 (GORM Structs)
│   ├── user.go
│   ├── fortune_record.go
│   └── match_profile.go
├── middleware/              # 中间件目录 (Gin Middlewares)
│   ├── auth.go               # JWT 鉴权校验 (解析 Token，拦截未登录请求)
│   ├── cors.go               # 跨域处理 (放行 Vue 前端的跨域请求)
│   └── rate_limit.go         # 基于 Redis 的接口限流 (防止被人疯狂刷算命接口)
├── route/                   # 路由层：注册 Gin 路由
│   └── router.go             # 统一定义 /api/v1/user, /api/v1/fortune 等路由组
├── mq/                       # RabbitMQ 消息队列处理
│   ├── producer.go           # 生产者：API 接收到请求后，将任务 Push 到队列
│   └── consumer.go           # 消费者：Worker 进程 Pull 任务，调用大模型，写回 DB
├── pkg/                      # [MVC - V 的替代] 公共工具与脚手架包
│   ├── postgres/             # PostgreSQL 初始化 (GORM)
│   ├── redis/                # Redis 客户端初始化
│   ├── rabbitmq/             # RabbitMQ 连接池管理
│   ├── response/             # 统一 JSON 返回格式 (如 {code: 0, msg: "success", data: ...})
│   ├── jwt/                  # Token 生成与解析工具
│   └── utils/                # 杂项工具 (如密码 Hash、时间格式化等)
├── go.mod
└── go.sum